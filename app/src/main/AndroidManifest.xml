<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden"
            android:label="@string/app_name">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Собственный MIME-тип пакета: самый точный вариант.
                 Атрибуты <data> внутри одного фильтра всё равно комбинируются
                 системой, поэтому объявляем их по одному на тег. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/vnd.eva.kb+zip" />
            </intent-filter>

            <!-- Файлы с расширением .evakb, когда путь виден в URI и тип НЕ указан:
                 фильтр без mimeType совпадает только с интентами без типа. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:pathPattern=".*\\.evakb" />
            </intent-filter>

            <!-- То же расширение, но для интентов С типом: VK и часть файловых
                 менеджеров подставляют MIME (zip/octet-stream/text/plain/…), а
                 фильтр выше такие интенты не ловит вообще. Расширение всё равно
                 обязано быть .evakb, поэтому чужие файлы мы на себя не забираем. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:pathPattern=".*\\.evakb" />
                <data android:mimeType="*/*" />
            </intent-filter>

            <!-- Расширения в URI не видно (частый случай для вложений VK): тогда
                 остаётся только тип. Ограничиваемся content:// и проверяем
                 содержимое уже в приложении. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" />
                <data android:mimeType="application/zip" />
                <data android:mimeType="application/x-zip-compressed" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ViewerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:label="@string/app_name" />
    </application>
</manifest>
